<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-1">
      <h2>Following User</h2>
      <% if @users.exists? %>
        <%= render 'index',users: @users %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>